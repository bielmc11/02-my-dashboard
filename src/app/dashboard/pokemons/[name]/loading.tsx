export default function Loading() {
    // You can add any UI inside Loading, including a Skeleton.
    return (
        <h4 className="text-3xl bg-red-400">La pagina esta cargando desd el archivo loanding</h4>
    )
  }